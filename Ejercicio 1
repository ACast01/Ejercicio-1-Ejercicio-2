# Solicitar al usuario ingresar peso en kilogramos y altura en metros
peso = float(input("Ingresa tu peso en kilogramos: "))
altura = float(input("Ingresa tu altura en metros: "))

# Calcular el IMC
imc = peso / (altura ** 2)

# Imprimir la categoría basada en el IMC
if imc < 18.5:
    categoria = "Bajo peso"
elif imc < 25:
    categoria = "Peso normal"
elif imc < 30:
    categoria = "Sobrepeso"
else:
    categoria = "Obesidad"

print("Tu IMC es:", imc)
print("Categoría:", categoria)
